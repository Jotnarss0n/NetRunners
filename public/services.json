{
  "services": [
    {
      "id": "smb_scan",
      "name": "SMB Vulnerability Scan",
      "protocol": "SMB",
      "command": "nmap -p{PORT} --script smb-vuln* {IP}",
      "description": "Scans for SMB vulnerabilities using Nmap scripts."
    },
    {
      "id": "ftp_enum",
      "name": "FTP Enumeration",
      "protocol": "FTP",
      "command": "nmap -p{PORT} --script ftp-anon,ftp-bounce,ftp-libopie,ftp-proftpd-backdoor {IP}",
      "description": "Enumerates FTP service for anonymous access and vulnerabilities."
    },
    {
      "id": "http_scan",
      "name": "HTTP Directory Scan",
      "protocol": "HTTP",
      "command": "gobuster dir -u http://{IP}:{PORT} -w /usr/share/wordlists/dirb/common.txt",
      "description": "Scans for directories on an HTTP server using Gobuster."
    }
  ]
}
